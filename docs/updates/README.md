# docs/updates/

Generated Android-update audit reports written by `tools/audit-android-update.ps1`
(read-only). Filenames look like `20260624T134508Z-android-9bad830-audit.md`.

These reports are **generated artifacts** and are git-ignored (see `.gitignore`):
they are reproducible by re-running the audit script and contain raw Git evidence
plus a placeholder for Claude's later semantic classification. This `README.md` is
tracked so the directory exists; the reports themselves are not committed.
